# Use python scripts/setup.py to create .env with random local secrets.
POSTGRES_PASSWORD=replace-with-random-url-safe-password
JWT_SECRET=replace-with-a-unique-secret-of-at-least-32-characters
ENVIRONMENT=development
DEMO_MODE=true
APP_ORIGIN=http://localhost:3000
COOKIE_SECURE=false
# Optional gateway contract: docs/AI-GATEWAY.md
TRIAGE_MODEL_URL=
TRIAGE_MODEL_KEY=
TRIAGE_MODEL_NAME=
